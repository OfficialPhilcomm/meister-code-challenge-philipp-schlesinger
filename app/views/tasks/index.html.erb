<%= turbo_frame_tag dom_id(@project, "tasks") do %>
  <div class="grid grid-cols-2 gap-4 p-4">
    <% @tasks.each do |task| %>
      <div class="border-2 border-blue-500 rounded-md p-1 bg-blue-50">
        <%= task.name %>
      </div>
    <% end %>
  </div>
<% end %>
